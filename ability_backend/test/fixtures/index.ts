import * as sign from './sign.fixtures';
import * as user from './user.fixtures';
import * as method from './method.fixtures';
import * as instrument from './instrument.fixtures';

export { sign, user, method, instrument };
